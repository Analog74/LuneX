-- SocialManager.lua
-- Social features for Game74

local SocialManager = {}

function SocialManager:SendMessage(player, message)
    -- Send chat message
end

function SocialManager:AddFriend(player, friendId)
    -- Add friend
end

return SocialManager
